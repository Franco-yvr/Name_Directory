package ca.ubc.cs.cpsc210.model;import java.util.ArrayList;public class WhereTag {    public ArrayList<String> wheretag;    // MODIFIES: this    // EFFECTS: Construct a tag to be used as a WhereTag entry    public WhereTag() {        this.wheretag = wheretag;    }    //MODIFIES:this    //EFFECTS: Convert a series of horizontally written tags into a list of tags    public void convertIntoList(String tag) {        wheretag = new ArrayList<String>();        while (hasNext(tag)) ;        if (next("@")) {            int starttag = tag.indexOf("@");            int endtag = tag.indexOf("@") - 1;            addTag(tag.substring(starttag, endtag));        }    }    //MODIFIES:this    //EFFECTS:Add new item to list of tags    public void addTag(String tag) {        wheretag.add(tag);    }    // MODIFIES: this    // EFFECTS: Return tag when index called    public String getTag(int index) {        return wheretag.get(index);    }    //REQUIRE: tag list must not be empty    //EFFECTS: Search the tag list for the keywords entered in the user interface search box.    //Return true is keyword is found.    public boolean searchWhereTag(String searched) {        int i;        for (i = 0; i == wheretag.size() - 1; i++) {            if (wheretag.get(i).contains(searched)) {                return true;            }        }        return false;    }    // MODIFIES: this    // EFFECTS: removes tag from list given index    //public void removeTag(int index) {    //    wheretag.remove(index);    // }    // EFFECTS: Return true is the WhereTag strings contains given letter    //public boolean indexOf(String searched) {    //   return wheretag.indexOf(searched);    //}}